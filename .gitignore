# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller (сборка exe)
build/
dist/
*.manifest

# Личный конфиг лаунчера — создаётся сам при первом запуске
# (кэш установленных расширений, недавние папки, последний выбор).
launcher_config.json
launcher_config.json.*.tmp
launcher.log
launcher.log.*
# Бэкапы PATH, которые лаунчер пишет рядом с конфигом перед чисткой.
path_backup_*.txt
machine_path_backup_*.txt

# ОС / редакторы
Thumbs.db
desktop.ini
.DS_Store
.idea/
.vscode/
